# Streamlit Cloud Secrets 配置模板
#
# 重要：此文件仅供参考，真实密钥请在 Streamlit Cloud 控制台配置
# 路径：https://share.streamlit.io/ → 选择应用 → Settings → Secrets
#
# 复制以下内容到 Secrets 编辑器，并填入真实值

# OpenAI 兼容 API 配置（必需）
OPENAI_API_KEY = "sk-your-api-key-here"
OPENAI_BASE_URL = "https://newapi.deepwisdom.ai/v1"
OPENAI_MODEL = "gpt-4o"
LLM_PROVIDER = "openai"

# 可选：存储路径覆盖（Cloud环境使用默认即可）
# WEFINANCE_STORAGE_FILE = "/tmp/.wefinance/data.json"

# 可选：时区配置（日志时间显示）
TZ = "Asia/Shanghai"
